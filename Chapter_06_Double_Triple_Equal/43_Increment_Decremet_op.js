// Pre Increment

//let a = 10;
//let b = ++a; // (Increment the a by 1)

// Pre Increment -> ++a -> Increase the value and then assign.
//console.log( a );

//console.log( b );


// Pre Decrement

//let a = 10;
//let b = --a; // (Decrement the a by 1)

// Pre Decrement -> --a -> Decrease the value and then assign.
//console.log( a );

//console.log( b );

//Post Decrement

let a = 10;
let b = a--; // (Decrement the a by 1)

// Pre Decrement -> --a -> Decrease the value and then assign.
console.log( a );

console.log( b );