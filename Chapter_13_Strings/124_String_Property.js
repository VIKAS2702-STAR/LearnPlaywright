console.log( typeof ( "200" ) );
let str = "Hello, World!"; // length will count space and comma in between of the string
console.log( str.length ); // Length will always start from 1, and total length-13

// Access of Index
console.log( str[0] );
console.log( str[7] );
console.log( str.at( -1 ) );
console.log( str.at( -6 ) );

// Index will always start from 0
// Length will always start from 1

console.log( str.charAt( 0 ) );
console.log( str.charCodeAt( 0 ) );


